### required todo
- only take AC & BD to result, to avoid translation error affect score
- 更具體的 A/B/C/D 範例。劉博士希望看到每張圖片實際丟入 BERTScore 的 input 長什麼樣，包括 baseline、caption、翻譯版本與欄位內容，而不只看分數表格。
- BERTScore calibration:
  - we already know BERTscore only count content, it won't affect by harness schema.
  - However, must 檢查 BERTScore 是否被短欄位或固定 wound feature 詞彙拉高。若 visual summary 加上 wound features 一起評估，可能因為某些短詞或固定回答一致，而讓分數偏高。因此後續需比較："只使用 visual summary 的 BERTScore。" vs "使用 visual summary + wound features 的 BERTScore。(若兩者差距明顯，需說明分數是否被 wound features 膨脹。)
- VQA dataset : 將模型生成的 visual summary 轉為選擇題，讓 LLM 根據描述判斷傷口類型。例如根據紅斑、表皮損失、粗糙、紅腫等描述，判斷屬於擦傷、刮傷或切傷。這可用於建立一組可被量化的問題與答案VQA-dataset，協助驗證 LLM 對傷口描述的理解是否準確。


### planned idea
- reduce answer schema
  - current answer is too long
  - actually manually check & future application might only use some necessary part. This have to evaluate&adjust.
- import self-supervised related method, to improve prompt-design by model itself. 
  - we already found suitable score--"BERTScore"
  - Essentialiy, current prompt design is very simple, it required improvement. Current design is refer to peompt-engineering related paper. It's good but time-consuming. And i'ma not good in paper finding. 
  - i am actually curious of how other SOTA promp-engineering proj design&improve their work?
- [autoresearch](https://github.com/karpathy/autoresearch) is a self-supervised related work.(Isn't it)  
  - It originally use to find best "parameter value", and we are find "prompt design", it a bit different.
  - I'm interesting in it "auto", "AI self improvement" way, we can design our own mechanism anb go for this direction
  - Our hardware: this computer (4090 GPU)
  - the "point" i figure out now, which is we can use in autoresearch, is "ablatoin test". That is, seperate prompt-design to different part, and use autoresearch method to self-improve 
- other past but not applied yet idea
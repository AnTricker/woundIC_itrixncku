# 720

### bertScore rescale
> ⚠️set robust scoring machnism first will make diff LLM test meaningful & efficient
- (ref) cosine similarity(-1~1)->readable score(0-1)
- (ref) what score range is "Good"?
#### 720 meet
  - 論文原文提供的baseline >0 => [bert_score/rescale_baseline/en/roberta-large.tsv](https://github.com/Tiiiger/bert_score/blob/master/bert_score/rescale_baseline/en/roberta-large.tsv)
  - full caption) 某些key的 "not observed" 有/無 出現在 ref/can，共會出現4象限=> 將此分離出來分析, manual驗證真正狀況
  - full caption) 研究現在code的真正計算過程? (推測) 現在是將所有key-dic的 dic string 混在一起算平均
      - 7傷口 每個key 單獨挑出來，各自算其bertscore，畫box-plot 
      - => 看每個文字生成key於各類傷口 各自分數分布(Is整體集中差? 分散有高有低?)；找分數好的&分數差的，分析prompt調整方向


### optimize prompt design
- remove useless part, reduce I/O token
- try use Medgemma to improve wound-specific discription
- reduce output part, make diff LLMs comparison easier

### medical-related local model
- import medgemma:3b & medgemma:27b
- import gemma4:12b
